#include <stdio.h>
#include <stdlib.h>
int main()
{
	char a[200];
    int alpha[26];
    int i;
    int lena;
    gets(a);
    lena=strlen(a);
    for(i=0;i<lena;i++)
		if()
	system("pause");
	return 0;
}
