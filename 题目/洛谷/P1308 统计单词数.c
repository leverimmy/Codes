#include <stdio.h>
#include <stdlib.h>
int main()
{
	char a[10];
    char b[100];
    gets(a);
    gets(b);
    lena=strlen(a);
    lenb=strlen(b);
    for(i=0;i<lenb;i++)
		if(isalpha(b[i])==1)
			b[i]=b[i]+32;
    for(i=0;i<n;i++)
		if
	system("pause");
	return 0;
}
