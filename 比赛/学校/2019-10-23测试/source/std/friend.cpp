#include <bits/stdc++.h>

using namespace std;
int _w;

int main() {
		freopen("friend.in","r",stdin);
	freopen("friend.out","w",stdout);	
	int T;
	_w = scanf( "%d", &T );
	while( T-- ) puts("0!");
	return 0;
}
