#include <bits/stdc++.h>
#include <windows.h>
#define rgi register int
#define fin(s) freopen(s,"r",stdin)
#define fout(s) freopen(s,"w",stdout)
using namespace std;
signed main()
{
	ios::sync_with_stdio(false);
	cin.tie(0),cout.tie(0);
	fout("14.out");
	int cnt=0;
	for(rgi i=1;i<=100000000;++i)
		++cnt;
	cout<<"5 2 1 3 4"<<endl;
	return 0;
}

