#include <bits/stdc++.h>
#define rgi register int
using namespace std;
const int maxn=1e5+5;
int t,n,a[maxn],fa[maxn];
signed main()
{
	ios::sync_with_stdio(false);
	cin.tie(0),cout.tie(0);
	//freopen("tree.in","r",stdin);
	//freopen("tree.out","w",stdout);
	for(cin>>t;t;--t)
	{
		cin>>n;
		for(rgi i=1;i<=n;++i)
			cin>>a[i];
		
	}
	return 0;
}
