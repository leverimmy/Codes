#include <cstdio>
#include <cstring>
#include <algorithm>
#include <cmath>
#define rgi register int
#define il inline
#define ll long long

using namespace std;

int main()
{
	n = read();
	for(rgi i = 1; i <= n; ++i)
		a[i] = read();
	for(rgi i = 1; i <= n; ++i)
		b[i] = read();
	for(rgi k = 1; k <= n; ++k)
	{
		double sum=0;
		for(rgi i=1;i<=k;++i)
			sum+=fabs()
	}
	return 0;
}

